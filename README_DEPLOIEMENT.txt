avoir à disposition les deux images (machine apache & machine bdd)
avoir à disposition un système de machine virtuelle (local ou cloud) tel que (vmware, virtual box, azure, aws, google cloud...)
mettre la machine de la bdd et apache dans le même réseau
reconfigurer l'ip des machines
regarder dans le fichier de config de la base de donnée pour mettre la bonne ip
configurer le port de sortie afin qu'il soit accessible depuis l'exterieur de la machine.

Toute ces informations sont générale, il suffira d'une simple recherche avec l'outil que vous utiliserez pour héberger le site